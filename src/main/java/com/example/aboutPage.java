package com.example;

import org.openqa.selenium.WebDriver;

public class aboutPage {
	WebDriver driver;
	 
	 public aboutPage(WebDriver driver) {
	        this.driver = driver;
	       	        
	 }
}